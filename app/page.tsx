// Default board page — renders the ViewSwitcher (Kanban / Calendar / Dashboard)
import { ViewSwitcher } from "@/components/layout/ViewSwitcher";

export default function HomePage() {
  return <ViewSwitcher />;
}
